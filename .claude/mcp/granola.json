{
  "name": "granola",
  "description": "Meeting intelligence from Granola transcripts",
  "server": {
    "command": "python",
    "args": ["core/mcp/granola_server.py"],
    "env": {
      "VAULT_PATH": "${workspaceFolder}"
    }
  },
  "notes": "Uses the official Granola public API (https://public-api.granola.ai). Requires a Granola Business plan and a GRANOLA_API_KEY — set it up with /granola-setup."
}
