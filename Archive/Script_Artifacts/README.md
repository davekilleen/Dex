# Script Artifacts (Archived)

One-off scripts and data snapshots moved out of `.scripts/` on 2026-07-07.
These are **completed work products**, not live tools — kept for reference only.

- `outreach-*.ps1 / .md` — dated outreach email batches, already sent (June–July 2026)
- `create-outlook-drafts.ps1`, `push-outreach-to-outlook.ps1`, `reformat-*.ps1`,
  `HOW_TO_PUSH_OUTLOOK_DRAFTS.md` — the old manual Outlook-drafts workflow,
  superseded by the `/email-drafts-custom` dashboard (v1.22.0)
- `migrate-projects-*.ps1`, `migration-plan.csv`, `migration-log.txt` — the
  account-centric Projects/ restructure, already executed
- `pipeline-*.json` — point-in-time Salesforce pipeline snapshots (June 2026);
  **do not treat as current data**, use `/pipeline-sync` instead
- `rename-vendor-folders*.py` — completed vendor folder rename migration

Live tools still in `.scripts/`: `sf-pull-sync.py`, `sf-activity-list.py`,
`sf-activity-sync.py`, `case-alert.py`, `pipeline-sync-*.py`, and the
`salesforce-mcp/`, `customer-intel/`, `email-drafts/` integrations.
