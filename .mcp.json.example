{
  "mcpServers": {
    "task-mcp": {
      "type": "stdio",
      "command": "python",
      "args": [
        "{{VAULT_PATH}}/core/mcp/task_server.py"
      ],
      "env": {
        "VAULT_PATH": "{{VAULT_PATH}}"
      }
    },
    "calendar-mcp": {
      "type": "stdio",
      "command": "python",
      "args": [
        "{{VAULT_PATH}}/core/mcp/calendar_server.py"
      ],
      "env": {
        "VAULT_PATH": "{{VAULT_PATH}}"
      }
    },
    "granola-mcp": {
      "type": "stdio",
      "command": "python",
      "args": [
        "{{VAULT_PATH}}/core/mcp/granola_server.py"
      ],
      "env": {
        "VAULT_PATH": "{{VAULT_PATH}}"
      }
    }
  }
}
