{
  "adapter_schema_version": "1.0.0",
  "harness_id": "cowork",
  "status": "native",
  "kind": "claude-plugin",
  "native_paths": [".claude-plugin/plugin.json", "skills/", "hooks/hooks.json"],
  "example": {
    "manifest": "packages/dex-agent-plugin/.claude-plugin/plugin.json",
    "note": "Upload or install the reviewed Claude plugin. Cowork external connectors require a public internet endpoint, so the local stdio MCP bridge is not claimed here."
  }
}
