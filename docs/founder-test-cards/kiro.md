<!-- Generated by scripts/generate-founder-test-cards.py. Do not edit by hand. -->

# Written host path: `kiro`

Unreleased. Not a live install. Do not publish. Do not merge.

**Lab issue (leave open):** https://github.com/davekilleen/dex-product-gtm-lab/issues/484

**Adapter source:** `core/harnesses/adapters/kiro.json`

## Adapter text (quoted — source of truth)

> Open the Dex folder in Kiro. Open the Powers panel, choose Add Custom Power, then Import power from a folder, and select packages/dex-agent-plugin. Name Dex in chat so Kiro wakes the shipped power — the package already lists dex in its keywords. If Dex guessed VS Code, Cursor, or nothing, confirm Kiro in /setup. Hooks are not part of this path. Ubuntu Cloud is not a person opening Kiro. A detection test or green CI is not a live install.

## Named adapter fields

- `local_package`: `./packages/dex-agent-plugin`
- `manifest`: `packages/dex-agent-plugin/plugin.json`
- `mcp_file`: `mcp.json`

## Steps

### Step 1

1. Open the Dex folder in Kiro.

**What you should see:** The Dex folder is open in that editor. This is still a written path, not a live install.

- [ ] I saw that.

**If this fails, send back this exact sentence:** `kiro step 1 failed: The Dex folder is open in that editor. This is still a written path, not a live install.`

### Step 2

2. Open the Powers panel, choose Add Custom Power, then Import power from a folder, and select packages/dex-agent-plugin.

**What you should see:** The named action is possible on this machine. This is still a written path, not a live install.

- [ ] I saw that.

**If this fails, send back this exact sentence:** `kiro step 2 failed: The named action is possible on this machine. This is still a written path, not a live install.`

## Limits from the adapter (not steps)

- Name Dex in chat so Kiro wakes the shipped power — the package already lists dex in its keywords.
- If Dex guessed VS Code, Cursor, or nothing, confirm Kiro in /setup.
- Hooks are not part of this path.
- Ubuntu Cloud is not a person opening Kiro.
- A detection test or green CI is not a live install.

## After the last checkbox

If every box is checked, send this exact sentence:

`kiro written path matched the adapter. Not a live install.`

If any box is unchecked, send only the failure sentence from that step. Do not continue past a failed step. Do not publish. Do not invite anyone.

## Honest close

Nobody has walked this path. This is not a live install. Do not publish. Do not sign, store, or invite anyone.
