<!-- Generated by scripts/generate-founder-test-cards.py. Do not edit by hand. -->

# Written host path: `vscode`

Unreleased. Not a live install. Do not publish. Do not merge.

**Lab issue (leave open):** https://github.com/davekilleen/dex-product-gtm-lab/issues/484

**Adapter source:** `core/harnesses/adapters/vscode.json`

## Adapter text (quoted — source of truth)

> Open the Dex folder in VS Code. Agent plugins stay off until you turn them on: set chat.plugins.enabled to true, then add chat.pluginLocations so ./packages/dex-agent-plugin is true. Reload VS Code. Confirm Dex skills and the bundled MCP tools appear. If Dex guessed Cursor, Kiro, or nothing, confirm VS Code in /setup. The family PreToolUse refusal file is in the shipped package. Ubuntu Cloud is not a person opening VS Code. A detection test or green CI is not a live install.

## Named adapter fields

- `enable_setting`: `chat.plugins.enabled`
- `plugin_locations_setting`: `chat.pluginLocations`
- `plugin_locations_entry`: `./packages/dex-agent-plugin`
- `local_package`: `./packages/dex-agent-plugin`
- `manifest`: `packages/dex-agent-plugin/plugin.json`
- `mcp_file`: `mcp.json`

## Steps

### Step 1

1. Open the Dex folder in VS Code.

**What you should see:** The Dex folder is open in that editor. This is still a written path, not a live install.

- [ ] I saw that.

**If this fails, send back this exact sentence:** `vscode step 1 failed: The Dex folder is open in that editor. This is still a written path, not a live install.`

### Step 2

2. Agent plugins stay off until you turn them on: set chat.plugins.enabled to true, then add chat.pluginLocations so ./packages/dex-agent-plugin is true.

**What you should see:** The named setting is on this machine. This is still a written path, not a live install.

- [ ] I saw that.

**If this fails, send back this exact sentence:** `vscode step 2 failed: The named setting is on this machine. This is still a written path, not a live install.`

### Step 3

3. Reload VS Code.

**What you should see:** The host reopens after the reload. This is still a written path, not a live install.

- [ ] I saw that.

**If this fails, send back this exact sentence:** `vscode step 3 failed: The host reopens after the reload. This is still a written path, not a live install.`

### Step 4

4. Confirm Dex skills and the bundled MCP tools appear.

**What you should see:** The named action is possible on this machine. This is still a written path, not a live install.

- [ ] I saw that.

**If this fails, send back this exact sentence:** `vscode step 4 failed: The named action is possible on this machine. This is still a written path, not a live install.`

## This editor stays advisory

Safety on this editor stays advisory. The family PreToolUse refusal file is in the shipped package, but this page does not claim a live intercept. The bundled safety check remains advisory unless a trusted pre-tool hook actually blocks the action. Nobody has walked this path. This is not a live install.

## Limits from the adapter (not steps)

- If Dex guessed Cursor, Kiro, or nothing, confirm VS Code in /setup.
- The family PreToolUse refusal file is in the shipped package.
- Ubuntu Cloud is not a person opening VS Code.
- A detection test or green CI is not a live install.

## After the last checkbox

If every box is checked, send this exact sentence:

`vscode written path matched the adapter. Not a live install.`

If any box is unchecked, send only the failure sentence from that step. Do not continue past a failed step. Do not publish. Do not invite anyone.

## Honest close

Nobody has walked this path. This is not a live install. Do not publish. Do not sign, store, or invite anyone.
