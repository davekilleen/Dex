<!-- Generated by scripts/generate-founder-test-cards.py. Do not edit by hand. -->

# Written host path: `copilot-cli`

Unreleased. Not a live install. Do not publish. Do not merge.

**Lab issue (leave open):** https://github.com/davekilleen/dex-product-gtm-lab/issues/484

**Adapter source:** `core/harnesses/adapters/copilot-cli.json`

## Adapter text (quoted — source of truth)

> From the Dex checkout, run `copilot plugin install ./packages/dex-agent-plugin`. Confirm Dex appears in `copilot plugin list`. Start the CLI from the Dex folder so the vault is the current working directory. The family PreToolUse refusal file is in the shipped package. Ubuntu Cloud is not a person opening this terminal. A detection test or green CI is not a live install.

## Named adapter fields

- `install_command`: `copilot plugin install ./packages/dex-agent-plugin`
- `inspect_command`: `copilot plugin list`
- `vault_grant`: `Start the CLI from the Dex folder so the current working directory is the vault.`
- `local_package`: `./packages/dex-agent-plugin`
- `direct_install_cache`: `~/.copilot/installed-plugins/_direct/`
- `manifest`: `packages/dex-agent-plugin/plugin.json`
- `mcp_file`: `mcp.json`

## Steps

### Step 1

1. From the Dex checkout, run `copilot plugin install ./packages/dex-agent-plugin`.

**What you should see:** The named command finishes. This is still a written path, not a live install.

- [ ] I saw that.

**If this fails, send back this exact sentence:** `copilot-cli step 1 failed: The named command finishes. This is still a written path, not a live install.`

### Step 2

2. Confirm Dex appears in `copilot plugin list`.

**What you should see:** Dex is listed by that inspect command. This is still a written path, not a live install.

- [ ] I saw that.

**If this fails, send back this exact sentence:** `copilot-cli step 2 failed: Dex is listed by that inspect command. This is still a written path, not a live install.`

### Step 3

3. Start the CLI from the Dex folder so the vault is the current working directory.

**What you should see:** The current working directory is the Dex folder. This is still a written path, not a live install.

- [ ] I saw that.

**If this fails, send back this exact sentence:** `copilot-cli step 3 failed: The current working directory is the Dex folder. This is still a written path, not a live install.`

## Family PreToolUse refusal (fixture, not a live walk)

A fixture sent a destructive command through the family PreToolUse protocol. The gate refused it (`permissionDecision: deny`, exit 2). That is a fixture, not a person opening this host. Nobody has walked this path. This is not a live install.

## Limits from the adapter (not steps)

- The family PreToolUse refusal file is in the shipped package.
- Ubuntu Cloud is not a person opening this terminal.
- A detection test or green CI is not a live install.

## After the last checkbox

If every box is checked, send this exact sentence:

`copilot-cli written path matched the adapter. Not a live install.`

If any box is unchecked, send only the failure sentence from that step. Do not continue past a failed step. Do not publish. Do not invite anyone.

## Honest close

Nobody has walked this path. This is not a live install. Do not publish. Do not sign, store, or invite anyone.
